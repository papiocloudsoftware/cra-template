import { makeStyles } from "@mui/styles";

export const useCommonStyles = makeStyles({
  flexCentered: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
  },
  fill: {
    width: "100%",
    height: "100%"
  }
});
